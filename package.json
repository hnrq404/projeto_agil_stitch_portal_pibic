{
  "name": "portal-pibic",
  "version": "1.0.0",
  "private": true,
  "description": "Portal de Gestão de Laboratório — monorepo (portal-lab/ frontend+Convex, server/ API de referência da Sprint 2)",
  "scripts": {
    "test": "npm test --prefix server",
    "test:unit": "npm run test:unit --prefix server",
    "test:integration": "npm run test:integration --prefix server",
    "test:e2e": "npm run test:e2e --prefix server",
    "typecheck": "npm run typecheck --prefix server"
  }
}
