{
  "name": "portal-pibic",
  "version": "1.0.0",
  "private": true,
  "description": "Portal de Gestão de Laboratório — Sprint 2 (Edital & Publicação): API Node/Prisma + SPA React",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix web && npm run db:push --prefix server && npm run db:seed --prefix server && npm run build --prefix web",
    "dev": "npm run dev --prefix server",
    "build": "npm run build --prefix web",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix web",
    "test": "npm test --prefix server",
    "test:unit": "npm run test:unit --prefix server",
    "test:integration": "npm run test:integration --prefix server",
    "test:e2e:api": "npm run test:e2e --prefix server",
    "test:e2e:ui": "npm run test:e2e --prefix web",
    "db:seed": "npm run db:seed --prefix server",
    "db:studio": "npm run db:studio --prefix server"
  }
}
